{% macro flash(type, message, close) %}
<div class="alert-message {{ type }}" data-alert="alert">
	{% if close|default(false) %}
	<a class="close" href="#">×</a>
	{% endif %}
	<p>
	{{ message | trans }}
	</p>
</div>
{% endmacro %}

{% macro session_flash(type, close) %}
{% set types = ['info', 'error', 'warning', 'success'] %}
{% if close is not defined %}
{% set close = false %}
{% endif %}
{% if app.session.getFlashes()|length > 0 %}
<div class="row">
        <div class="span16">
            {% if type|default(false) %}
                {% if app.session.hasFlash(type) %}
                    {{ _self.flash(type, app.session.flash(type), close) }}
                {% endif %}
            {% else %}
                {% for type in types %}
                    {% if app.session.hasFlash(type) %}
                        {{ _self.flash(type, app.session.flash(type), close) }}
                    {% endif %}
                {% endfor %}
            {% endif %}
        </div>
</div>
{% endif %}
{% endmacro %}

