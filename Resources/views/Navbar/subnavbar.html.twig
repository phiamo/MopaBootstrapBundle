{% block subnavbar %}
<div class="subnav {% if subnavbar.getOption('fixedTop') %}navbar-fixed-top{% endif %}">

    {# SUB NAV template currently do not suppor collapse !!! #}
		{#<div class="container{% if subnavbar.getOption('isFluid') %}-fluid{% endif %}">
			<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>#}

            <div class="nav-collapse">
                {{ subnavbar.getMenu('leftmenu') ? knp_menu_render(subnavbar.getMenu('leftmenu'), {'currentClass': 'active', 'ancestorClass': 'active', 'allow_safe_labels': true}) : 'no left menu defined' }}
            </div>
</div>

{% endblock %}

